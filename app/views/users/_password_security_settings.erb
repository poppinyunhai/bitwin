	
<div class="personal-showbox hide" id="password_view">
	<div class="personal-content">
		<div class="paset-head">
			<font>登入密码修改：</font>
			<button class="bitwin-btn pa-btn">修改</button>
		</div>
		<div class="paset-content">
			<div class="paset-line">
				<input type="password" class="paset-input" id='trade-password' placeholder="原始密码"/>
				<input type="password" class="paset-input" id='trade-password' placeholder="新密码（6位数字以上）"/>
				<input type="password" class="paset-input"  id='trade-password-confirm' placeholder="请再输入一次" />
			</div>
		</div>
	</div>

	<div class="personal-content top-line">
		<div class="paset-head">
			<font>安全提示问题：</font>
			<font class="color-orange"><%= current_user.question.nil?? '还没设置': '可以直接编辑修改答案'%></font>
			<button class="bitwin-btn pa-btn"><%= current_user.question.nil?? '设置': '修改'%></button>
		</div>
		<div class="paset-content">
			<div class="paset-line">
				<%=select_tag 'question', options_from_collection_for_select(Question.all, :id, :description, current_user.question.try(:id)), class: "paset-input", :prompt => "请选择一个想要设置的安全问题"%>
				<input type="text" class="paset-input" id='answer-input' value='<%=current_user.answer.try :answer%>'/>
			</div>
		</div>
	</div>

	<div class="personal-content top-line">
		<div class="paset-head">
			<font>交易密码：</font>
			<font class="color-orange">还没设置</font>
			<button class="bitwin-btn pa-btn"><%=current_user.trade_hash.nil?? '设置':'修改'%></button>
		</div>
		<div class="paset-content">
			<div class="paset-line">
				<input type="password" class="paset-input" placeholder="交易密码（6位数字以上）" />
				<input type="password" class="paset-input" placeholder="请再输入一次" />
			</div>
		</div>
	</div>
</div>